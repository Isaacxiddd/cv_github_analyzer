{
  "name": "cv-github-analyzer",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "pnpm --prefix extension build",
    "dev": "pnpm --prefix extension dev",
    "test": "pnpm --prefix extension test",
    "typecheck": "pnpm --prefix extension typecheck",
    "lint": "pnpm --prefix extension lint"
  }
}
